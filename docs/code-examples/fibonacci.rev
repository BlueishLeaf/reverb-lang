var fibonacci = fn(n) begin
    if n == 1 then
        return 0
    end
    else if n == 2 then
        return 1
    end
    return fibonacci(n - 1) + fibonacci(n - 2)
end

var forLoop = fn(start, stop, f) begin
    var loop = fn(i, sequence) begin
        if i < stop then
            return loop(i + 1, push(sequence, f(i)))
        end
        return sequence
    end
    loop(start, [])
end

print(forLoop(1, 20, fibonacci))
